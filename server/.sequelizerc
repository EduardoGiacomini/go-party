require("dotenv").config();
const path = require("path");

module.exports = {
  "config": path.resolve(__dirname, "src", "application", "database", "config.js"),
  "migrations-path": path.resolve(__dirname, "src", "application", "database", "migrations"),
};
